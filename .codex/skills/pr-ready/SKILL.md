# PR Ready (Codex)

Before opening PR:

- tests green (`cargo test -p pith-core`)
- eval gate executed for behavior changes
- docs updated for user-visible changes
- PR template filled (`Summary`, `Related Issue`, `What Changed`, `Validation`, `Notes`)
