# pith (cargo wrapper)

This crate installs a `pith` binary via Cargo and runs the bundled Pith CLI through Node.js.

Requirements:

- Node.js available in `PATH` (`node`)

Usage:

```bash
cargo install --git https://github.com/AngeloCastro9/Pith pith
pith --help
```
